name: Deploy to VPS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v4

      - name: 🚚 Copie du code source (Front)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "."
          # CIBLE EXACTE : Vérifie que c'est bien vps-1-sites
          target: "/vps-1-sites/open-bar-front"
          # Important : Si le dossier existe, on écrase.
          rm: true

      - name: 🚀 Mise en place Orchestrator & Build
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            # 1. On s'assure que les dossiers existent (au cas où rm: true a tout cassé)
            mkdir -p /vps-1-sites/open-bar-orchestrator
            # Note : scp a déjà recréé le dossier front s'il l'avait supprimé

            # 2. On remet le docker-compose au bon endroit (car scp l'a mis dans front)
            cp /vps-1-sites/open-bar-front/docker-compose.yml /vps-1-sites/open-bar-orchestrator/docker-compose.yml
            
            # 3. On va dans le dossier Orchestrator
            cd /vps-1-sites/open-bar-orchestrator

            # 4. On s'assure que les données existent
            mkdir -p openbar_data

            # 5. Build & Start
            # Le --build est crucial ici
            docker compose up -d --build --remove-orphans
            
            # Nettoyage
            docker image prune -f